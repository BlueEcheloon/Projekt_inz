<form method="post" class="col-2" action="{{ path('app_groups_delete', {'id': group.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ group.id) }}">
    <button class="btn btn-danger"><i class="material-icons">delete</i></button>
</form>
