{{ form_start(form) }}
    <div>
        {{ form_errors(form) }}
    </div>
    <div class="row">
        <div class="col-lg-6 col-sm-12 col-md-12">
            {{ form_row(form.name) }}
        </div>
    </div>
    {{ form_widget(form) }}
    <button class="btn btn-warning">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
